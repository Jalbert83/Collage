# README #

This repository is meant for creating collages of faces. It needs a set of images containing faces and an image that would be the mask for the composition.

## Setup for Windows ##

It runs with python 3.9. And it also needs MSVC 2022 C/C++ for building some library dependencies.
